{% if CAPTCHA_HTML %}
<script type="text/javascript">
ajax.callback.user_register = function(data){
	$('#'+ data.mode).html(data.html);
};
</script>
{% endif %}
<p class="nav"><a href="{{ U_INDEX }}">{{ __('HOME') }}</a></p>

<form action="{{ S_PROFILE_ACTION }}" method="post">
{{ S_HIDDEN_FIELDS|raw }}

<table class="forumline">
<col class="row1">
<col class="row2">
<tbody class="pad_4">
<tr>
	<th colspan="2">{{ __('FORGOTTEN_PASSWORD') }}</th>
</tr>
<tr>
	<td colspan="2" class="row2 small">{{ __('ITEMS_REQUIRED') }}</td>
</tr>
<tr>
	<td>{{ __('EMAIL_ADDRESS') }}: *</td>
	<td><input type="text" class="post" name="email" size="50" maxlength="255" /></td>
</tr>
{% if CAPTCHA_HTML %}
<tr>
	<td>{{ __('CAPTCHA') }}: *</td>
	<td>{{ CAPTCHA_HTML|raw }}</td>
</tr>
{% endif %}
<tr>
	<td colspan="2" class="catBottom">
		<input type="submit" name="submit" class="main" value="{{ __('SUBMIT') }}" />&nbsp;
		<input type="reset" name="reset" class="lite" value="{{ __('RESET') }}" />
	</td>
</tr>
</tbody>
</table>

</form>
